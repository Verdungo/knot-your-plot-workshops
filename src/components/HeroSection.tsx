import React from 'react';

const HeroSection = () => {
    return (
        <section>
            <h1>
                <span style={{ color: '#FF0000' }}>head later.</span>
            </h1>
        </section>
    );
};

export default HeroSection;